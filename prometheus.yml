global:
  scrape_interval: 15s       # how often Prometheus polls /metrics
  evaluation_interval: 15s

scrape_configs:

  - job_name: "model-api"
    static_configs:
      - targets: ["api:8000"]   # "api" = the service name in docker-compose
    metrics_path: /metrics
